import React from "react";

const Flex = ({ children, className }) => {
  return <div className={className} style={{ background: 'linear-gradient(135deg, #FDC830, #F37335)' }}>{children}</div>;
};

export default Flex;
